============
Installation
============

At the command line::

    $ easy_install django-consul-cache

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-consul-cache
    $ pip install django-consul-cache
